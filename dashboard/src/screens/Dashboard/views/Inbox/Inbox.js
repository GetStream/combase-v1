import React from "react";
import styled from "styled-components";

// Components ///
const Root = styled.div`
  flex: 1;
  flex-direction: row;
`;
export default () => (
  <Root>
    <p>Inbox</p>
  </Root>
);
